{
  "name": "distributed-ecommerce-backend",
  "private": true,
  "scripts": {
    "start": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "config": "docker compose config"
  }
}

